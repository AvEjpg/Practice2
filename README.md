# Система учета заявок на ремонт климатического оборудования

Веб-приложение для автоматизации учета заявок на ремонт климатического оборудования с разграничением прав доступа и аналитикой.

## 📁 Структура проекта

```
Practice2/
├── requirements.txt           # Все зависимости проекта
├── backend/                   # FastAPI бэкенд
│   ├── auth.py               # Аутентификация и авторизация
│   ├── crud.py               # CRUD операции с БД
│   ├── database.py           # Настройки подключения к БД
│   ├── main.py               # Основной файл FastAPI
│   ├── models.py             # SQLAlchemy модели
│   ├── schemas.py            # Pydantic схемы
│   └── routers/              # Маршруты API
│       ├── auth.py          # Аутентификация
│       ├── client.py        # API для клиентов
│       ├── comments.py      # Комментарии
│       ├── qr.py            # Генерация QR-кодов
│       ├── requests.py      # Заявки
│       └── users.py         # Пользователи
│
├── frontend/                 # Flask фронтенд
│   ├── app.py               # Основное Flask приложение
│   ├── static/              # Статические файлы
│   │   └── custom.js       # JavaScript
│   └── templates/           # HTML шаблоны
│       ├── auth/           # Страницы авторизации
│       ├── client/         # Интерфейс заказчика
│       ├── comments/       # Комментарии
│       ├── errors/         # Страницы ошибок
│       ├── requests/       # Заявки
│       ├── users/          # Пользователи
│       ├── base.html       # Базовый шаблон
│       ├── index.html      # Главная страница
│       └── statistics.html # Статистика
│
├── отчеты/                   # Отчеты, диаграмма, руководство
├── data/                    # Ресурсы
└── README.md                # Этот файл
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Клонирование репозитория
git clone <repository-url>
cd Practice2

# Создание виртуального окружения (опционально)
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка базы данных

```bash
# Установите PostgreSQL и создайте базу данных
createdb climate_service

# Или используйте SQL
CREATE DATABASE climate_service;
```

### 3. Конфигурация

Убедитесь, что строка подключения в `backend/database.py` соответствует вашей настройке PostgreSQL:
```python
DATABASE_URL = "postgresql://postgres:123@localhost:5432/climate_service"
```

### 4. Запуск приложения

```bash
# В первом терминале - запуск бэкенда
cd backend
uvicorn main:app --reload

# Во втором терминале - запуск фронтенда
cd frontend
python app.py
```

### 5. Доступ к приложению

- **Frontend**: http://localhost:5000
- **Backend API**: http://localhost:8000
- **API документация**: http://localhost:8000/docs

## 📋 Тестовые пользователи

| Логин | Пароль | Роль |
|-------|--------|------|
| login1 | pass1 | Менеджер |
| login2 | pass2 | Специалист |
| login4 | pass4 | Оператор |
| login6 | pass6 | Заказчик |


## 🔧 Технологии

- **Backend**: FastAPI, SQLAlchemy, PostgreSQL, JWT
- **Frontend**: Flask, Bootstrap 5, JavaScript
- **База данных**: PostgreSQL
- **Аутентификация**: JWT токены

## 📊 Основные функции

1. **Управление заявками** - создание, редактирование, отслеживание статусов
2. **Разграничение прав** - 5 ролей пользователей с разными правами
3. **Статистика** - аналитика по заявкам, времени выполнения, типам оборудования
4. **QR-код обратной связи** - генерация QR-кода для опроса качества
5. **Комментарии** - общение между специалистами и клиентами

## 📞 Поддержка

При возникновении проблем проверьте:
1. Запущен ли PostgreSQL
2. Правильность строки подключения к БД
3. Установлены ли все зависимости
4. Логи приложения в консоли

---
